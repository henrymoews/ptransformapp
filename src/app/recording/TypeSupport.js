/**
 * Empty constants that are only used to get type support from Webstorm
 */

/**
 *
 * @returns {Segment[]}
 */
export function emptySegments() {
  return []
}

/**
 *
 * @returns {[number, number] | null}   [lat, lon]
 */
export function nullPosition() {
  return null
}
